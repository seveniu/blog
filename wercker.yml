box:
  id: debian
  cmd: /bin/sh
build:
  steps:
    - arjen/hugo-build@1.19.0

deploy:
  steps:
    - install-packages:
      packages: git ssh-client
    - seveniu.github.io/gh-pages@0.2.1:
      token: $GIT_TOKEN
      domain: seveniu.github.io
      basedir: public
